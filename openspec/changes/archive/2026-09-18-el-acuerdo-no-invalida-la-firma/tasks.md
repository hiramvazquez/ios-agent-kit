## 1. Los casos, en rojo antes que el código

- [x] 1.1 En `scripts/verifica-salidas.sh`, una sección nueva sobre un repositorio temporal con `kit.conf`, un `openspec/changes/x/tasks.md` y un `openspec/specs/y/spec.md` commiteados, y firma verde: editar el `tasks.md` deja `--comprueba` en 0; stagearlo también; `git commit` de eso pasa por el hook; `git mv` del cambio a `archive/` más una edición en `openspec/specs/` pasa `--comprueba`; tras eso, una edición fuera de `openspec/` lo pone en 1; una edición en `openspec-notas/` lo pone en 1. Verificación: los casos que esperan 0 salen en rojo con la huella de hoy, y se anota cuántos. Hecho: seis casos; con la huella de hoy salían en rojo los cuatro que esperan «pasa» (4 de 58); los dos que esperan «bloquea» ya pasaban, y vigilan que la exclusión no se ensanche

## 2. La huella

- [x] 2.1 `huella_diff` en `scripts/lib-kit.sh` con `-- ':(top)' ':(top,exclude)openspec'` en las tres llamadas a `git diff`, y su comentario con la exclusión, su razón y su límite. Verificación: `verifica-salidas.sh` en verde, y el resto de bancos con los mismos recuentos. Hecho: 58 de 58; los otros bancos con los mismos recuentos (36, 7, 10, 21)
- [x] 2.2 Comprobar que el hook generado lleva la exclusión (`grep ':(top,exclude)openspec' .agent-kit/pre-commit` tras verificar en un temporal) y que con `openspec/` limpio la huella nueva y la vieja coinciden. Verificación: anotar los dos números. Hecho: el hook generado en `.git/hooks/` y en `.agent-kit/` lleva `':(top,exclude)openspec'` en las tres llamadas; en este repositorio, con `openspec/` limpio, huella vieja y nueva `92d72852d5ce9b9c`

## 3. Decirlo donde se lee

- [x] 3.1 `docs/PIEZAS.md` (sección de `verifica.sh`), `commands/kit-verifica.md` y la cabecera de `scripts/verifica.sh`: la norma de stagear, verificar y commitear aparte vale para lo que está fuera de `openspec/`, y el límite va en PIEZAS. Verificación: `grep -n 'openspec/' docs/PIEZAS.md commands/kit-verifica.md scripts/verifica.sh`. Hecho: la norma y el límite en PIEZAS; `kit-verifica.md` y la cabecera de `verifica.sh` dicen «salvo `openspec/`» y remiten

## 4. Cierre

- [x] 4.1 Los criterios de aceptación del `proposal.md`, con su resultado anotado aquí. Resultado: los seis casos en verde (58 de 58) y los cuatro que esperan «pasa» salían en rojo con la huella anterior; huella vieja y nueva iguales con `openspec/` limpio (`9520b5a1107089d7` con código sucio y stageado; el revisor lo midió en 13 estados); una sola definición, y el hook generado la lleva con `--no-literal-pathspecs` y `openspec/`; PIEZAS, `kit-verifica.md`, FLUJO y los comentarios dicen «salvo `openspec/`» y el límite; `openspec validate --all` limpio; la verificación completa, verde sobre una copia del árbol
- [x] 4.2 Una ronda de `/kit-revisa`, con la ruta del cambio; decide el owner. Ronda 1 del revisor: RED · comportamiento: sí. Regresión de este cambio: con `GIT_LITERAL_PATHSPECS=1` (`git --literal-pathspecs`, magit), heredado por el hook, git leía el pathspec como un nombre, la huella salía vacía y pasaba código sin verificar. Arreglo: `git --no-literal-pathspecs diff` en las tres llamadas, y `openspec/` con barra final para que sea solo el directorio, como dice la cláusula 6. Reproducido con el hook de verdad en un temporal: código sin firmar con `--literal-pathspecs commit -am` bloquea (1), firmado pasa (0). Sin caso de banco nuevo: la regla del repo dice que un caso nace por un fallo que llegó a un proyecto real, y este no llegó. Además, dos textos que seguían diciendo que cualquier cambio invalida la firma (`kit-verifica.md` y FLUJO paso 8), y el borde declarado en PIEZAS, FLUJO y el delta: la firma vale hasta el commit que la sigue. El caso del `openspec/` anidado no se añade, por la misma regla; la exclusión con `:(top)` y barra final no lo excluye (medido). Como el arreglo cambia el código, segunda mirada del mismo revisor solo sobre el arreglo
- [x] 4.3 `/kit-verifica` en verde antes de archivar, y archivar y commitear sin volver a verificar: es la prueba en vivo de este cambio. Hecho: verificado con todo stageado, archivado, esta línea marcada y commiteado con la misma firma
