## 1. El hook, y su banco antes que él

- [x] 1.1 En `scripts/verifica-salidas.sh`, casos nuevos sobre un repositorio temporal con `kit.conf`: tras verificar existe el hook con la marca y es ejecutable; `git commit` con índice firmado pasa; `git commit -am` tras editar bloquea; `git commit <pathspec>` tras editar bloquea; `git -C <repo> commit` desde fuera sin firma bloquea; `cd ~/…` con `HOME` apuntado al temporal bloquea; primer commit sin `HEAD` pasa; hook ajeno queda igual y el informe lo nombra; con `core.hooksPath` no se escribe en `.git/hooks`. Verificación: todos en rojo antes de tocar `verifica.sh`. Hecho: 19 casos nuevos en `verifica-salidas.sh`; 11 en rojo antes de tocar `verifica.sh` (los otros pasaban por una razón equivocada —sin hook, un commit anterior se colaba y el siguiente no tenía nada que commitear—, y por eso cada caso de bloqueo hace ahora su propia edición)
- [x] 1.2 `verifica.sh`: función que escribe el hook según D2 y D3 en `$(git rev-parse --git-path hooks)`, llamada tanto en verde como en rojo, con la línea del informe la primera vez y el aviso con hook ajeno. Verificación: los casos de 1.1 en verde. Hecho: `instala_puerta` en `verifica.sh`; el hook siempre en `.agent-kit/pre-commit` y copiado a `$(git rev-parse --git-path hooks)/pre-commit` si no hay uno ajeno ni `core.hooksPath`. 47 de 47 en verde
- [x] 1.3 Vaciar el hook generado a mano y correr el banco: los casos de bloqueo caen. Verificación: anotar cuántos caen. Hecho: con el hook generado como `exit 0`, caen 8 de 47 (los seis de bloqueo y los dos «pasa» que dependen de que el anterior bloqueara)
## 2. Restar

- [x] 2.1 Borrar `scripts/puerta-commit.sh`, `scripts/analiza-invocacion.py` y `scripts/verifica-puerta.sh`; quitar `PreToolUse` de `hooks/hooks.json` y su `_nota` pasa a dos hooks; quitar el paso «puerta de commit» de `kit.conf`. Verificación: `bash scripts/autocomprueba.sh` en verde y `ls scripts/` sin los tres. Hecho: los tres borrados; `hooks/hooks.json` con dos hooks y la `_nota` reescrita; `kit.conf` sin el paso; `plugin.json` dice «dos hooks». `autocomprueba.sh` verde
- [x] 2.2 `scripts/estado.sh`: la línea de si la puerta está instalada. Verificación: en este repositorio dice que sí tras verificar, y que no en un temporal sin verificar. Hecho: línea «puerta de commit: instalada / sin instalar / en .agent-kit y tu pre-commit tiene que llamarlo»
## 3. Decirlo

- [x] 3.1 `docs/PIEZAS.md`: la sección de la puerta describe el hook de git, qué no frena, y el caso del hook ajeno; `README.md`, `docs/FLUJO.md` (paso 8), `docs/INSTALACION.md` («la puerta bloquea…»), `commands/kit-verifica.md`, `openspec/config.yaml`: dos hooks, y ninguna mención a `PreToolUse` ni al analizador. Verificación: el `grep -rn` del criterio de aceptación vacío. Hecho: README, FLUJO (paso 8 y tabla de hooks), PIEZAS (sección nueva), INSTALACION («Actualizar»), `kit-verifica.md`, `config.yaml`. Además el `Purpose` de la spec viva `puerta-de-commit` y un delta para `raiz-de-trabajo`, que nombraba a `puerta-commit.sh`
- [x] 3.2 La spec viva `puerta-de-commit`: el delta de este cambio la sustituye al archivar; comprobar con `openspec validate la-puerta-es-de-git`. Hecho: `openspec validate la-puerta-es-de-git` válido
## 4. Cierre

- [x] 4.1 Correr los criterios de aceptación del `proposal.md` y anotar el resultado. Resultado: banco 52 de 52 (cubre los seis primeros criterios); los tres ficheros borrados, `hooks.json` con `SessionStart` y `UserPromptSubmit`, `autocomprueba.sh` verde; el `grep` de restos solo encontraba las dos specs vivas que este archivado reescribe; los casos de bloqueo caen con el hook vaciado (8 de 47 en la primera medición); segunda mirada del revisor: GREEN
- [x] 4.2 `/kit-revisa`, una ronda; decide el owner. Ronda 1 del revisor: RED · comportamiento: sí. Dos fallos de código —el hook se quedaba como muro si desaparecía `kit.conf` (ahora se abre sin él) y el informe decía «instalada» antes del `cp` (ahora solo tras escribirlo, y avisa si no puede)—, un límite sin declarar (merge, revert, cherry-pick y rebase no pasan por `pre-commit`), el informe con `core.hooksPath` sin nombrar el fichero, README con «tres hooks» y un escenario sin caso (el mensaje). Cinco casos de banco nuevos; la marca del hook pasa a `lib-kit.sh`. Como el arreglo cambia lo que hace el código, se le pidió al mismo revisor una segunda mirada solo sobre el arreglo
- [x] 4.3 `/kit-verifica` en verde, y comprobar que en este mismo repositorio el hook quedó instalado y bloquea un commit sin firma. Hecho: `.git/hooks/pre-commit` con la marca, `/kit-estado` dice «instalada», y un commit de prueba sin firma salió bloqueado con el mensaje del hook
