{
  "_nota": "Dos hooks y ninguno más. Cada uno existe porque resuelve un fallo observado, no por simetría. La puerta de commit no es un hook de Claude Code: es un hook pre-commit de git que instala verifica.sh. Si algún día hay un tercero, tiene que traer escrito el fallo que lo motiva.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/inyecta-contexto.sh\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/inyecta-contexto.sh\""
          }
        ]
      }
    ]
  }
}
